import Vue, { VueConstructor, PluginFunction } from "vue";
import "./vue";
export declare const eventBus: object & Record<never, any> & Vue;
export default class VueEventBus {
  static install: PluginFunction<never>;
}
